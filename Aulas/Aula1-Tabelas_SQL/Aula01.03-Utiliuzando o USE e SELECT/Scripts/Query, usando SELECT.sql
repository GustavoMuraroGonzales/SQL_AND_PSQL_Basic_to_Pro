SELECT actor_id, first_name FROM actor

/* Nesse código nós acessamos as colunas actor_id e first_name da tabela actor. */

SELECT * FROM actor

/* Nesse código acima usando o * nós acessamos todas as coolunas da tabela actor.